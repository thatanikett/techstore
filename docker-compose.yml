services:
  app:
    image: busybox:1.36
    command:
      - sh
      - -c
      - |
        echo "Use docker compose -f docker-compose.dev.yml up --build for local simulation.";
        echo "AWS blue/green deployment uses external RDS and ElastiCache, not local Postgres/Redis containers.";
        exit 1
